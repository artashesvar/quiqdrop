[build]
builder = "nixpacks"

[deploy]
startCommand = "python main.py"
restartPolicyType = "on_failure"

# The bot runs a web server on $PORT to receive the Notion OAuth redirect.
# Railway automatically sets PORT and routes HTTPS traffic to it.
